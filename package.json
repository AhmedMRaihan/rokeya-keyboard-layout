{
  "name": "rokeya-keyboard-layout",
  "version": "4.4.73-5",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/chai": "^4.3.5",
    "@types/jquery": "^3.5.16",
    "@types/jsdom": "^21.1.1",
    "@types/mocha": "^10.0.1",
    "chai": "^4.3.7",
    "jquery": "^3.6.4",
    "jsdom": "^21.1.1",
    "mocha": "^10.2.0",
    "nyc": "^15.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  },
  "scripts": {
    "build": "tsc",
    "cleanup": "rm src/*.js || true",
    "build-watch": "npm run cleanup && tsc --watch",
    "test": "env TS_NODE_COMPILER_OPTIONS='{\"module\": \"commonjs\" }' nyc --reporter=text mocha -r ts-node/register tests/**/*.test.ts",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  }
}
