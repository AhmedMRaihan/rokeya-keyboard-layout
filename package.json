{
  "name": "rokeya-keyboard-layout",
  "version": "4.4.73-02072023",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/chai": "^4.3.6",
    "@types/jest": "^29.5.5",
    "@types/jsdom": "^21.1.5",
    "chai": "^4.3.10",
    "http-server": "^14.1.1",
    "jest": "^29.7.0",
    "jsdom": "^22.1.0",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "tsc",
    "cleanup": "rm src/*.js || true",
    "build-watch": "npm run cleanup && tsc --watch",
    "test": "npm run build && jest --colors --collectCoverage --verbose"
  }
}
